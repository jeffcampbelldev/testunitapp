# TestAppUnits

## Install and run on ios
```
git clone {REPO_URL}
cd {PROJECT_ROOT}
yarn
cd {PROJECT_ROOT/ios}
pod install
cd ..
yarn run ios
```

## Install and run on android
```
cd {PROJECT_ROOT}
yarn install
yarn run android
```
